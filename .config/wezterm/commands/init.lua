local toggle_transparency = require 'commands.toggle-transparency'
local toggle_theme = require 'commands.toggle-theme'
local work_layout = require 'commands.work-layout'

local M = {
  toggle_transparency,
  toggle_theme,
  work_layout
}

return M
